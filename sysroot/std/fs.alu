//! Working with files and directories

#[cfg(any(target_os="linux", target_os="macos", target_os="android"))]
{
    use unix::{
        OpenOptions, File, read_dir, DirIterator, DirEntry, FileType
    }
}
