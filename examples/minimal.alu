use std::random::{DEFAULT_RNG as rng, range};
use std::result::{try, Result};

protocol Equatable<T> {
    fn equals(a: &T, b: &T) -> bool;
}

fn compare<T: Equatable<T>>(a: T, b: T) -> bool {
    a == b
}

#[export]
fn main() {
    println!("{}", if compare(4, 4) { "true" } else { "false" });
}
