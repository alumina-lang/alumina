

struct ArrayWrapper<T> {
    inner: [T; 128],
    outer: [(T, T<ArrayWrapper<T>>); 128]
}

fn foo(a: u32) -> ArrayWrapper<ArrayWrapper<ArrayWrapper<u8>>> {
    //let a = point { x: 1, y: 2 + 2 };
    //a
}
