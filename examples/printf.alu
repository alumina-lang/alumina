// does not work yet.

use std::io::print;

macro printf(fmt, expr...) {
    std::io::printf(fmt, [(&expr as &())...])
}

fn printf(format: &[u8], args: &[&()]) {
    print(format);
    std::shed::write_number(args.len as i32);
}

#[export]
fn main() {
    printf("{} {} {}", 1, 2, 3);
}
