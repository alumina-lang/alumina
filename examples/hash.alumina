fn hash<u32>(value: u32) -> usize {
    value as usize
}

fn<T> hash<[T]>(value: T) -> usize {
    let result = 0;
    for let i = 0; i < value.len(); i += 1 {
        result = result * 37 + hash(value[i]);
    }
    result
}

fn hash<std::net::ip_address>(value: std::net::ip_address) -> usize {
    let result = 0;
    
    result
}
