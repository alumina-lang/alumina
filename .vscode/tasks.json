{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "quick",
            "type": "shell",
            "command": "make quick",
            "group": {
                "kind": "build"
            }
        },
        {
            "label": "build-stdlib-tests-release",
            "type": "shell",
            "command": "RELEASE=1 make build/release/stdlib-tests",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
