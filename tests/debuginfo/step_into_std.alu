// `step` enters the standard library (lldb skips `std::` by default; the
// formatters' module lifts that).
use std::collections::Vector;

fn main() {
    let v: Vector<i32> = Vector::new();
    v.push(1); // #break
    v.free();
}

// lldb: step
// check: stop reason = step in
// check: `std::collections::vector::Vector::push<i32>(self=
// check: value=1) at vector.alu
