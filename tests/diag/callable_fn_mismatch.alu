//! exit_code: 1
fn check<F: Fn(i32) -> i32>(f: F) -> i32 {  // diag: error(protocol_mismatch_detail): "type `named` does not match `Fn(i32) -> i32` (argument types do not match)"
    f(1)
}

fn named(x: i64) -> i32 {
    x as i32
}
fn main() {
    check(named);
}
