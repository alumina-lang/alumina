// ALUMINAC_FLAGS: --sysroot sysroot
// EXPECTED_ERROR: generic type parameters cannot be used in this context
// Generic arguments go on the function (`Option::none::<i32>()`), not on a
// type in the path, as in alumina-boot. (aluminac used to accept both.)

fn main() {
    let x = Option::<i32>::none();
}
