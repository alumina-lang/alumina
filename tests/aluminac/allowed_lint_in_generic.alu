// EXPECTED_QUIET
// An allowed lint says nothing, not even the notes on the generic
// instantiation it was found in.

#[allow(unnecessary_cast)]
fn three() -> i32 {
    let v: i32 = 3;
    v as i32
}

fn widen<T>(_x: T) -> i32 {
    three() + 1
}

fn main() -> i32 {
    widen(1u8) - 4
}
