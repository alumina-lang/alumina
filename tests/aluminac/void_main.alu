// Test that main functions with no return value work
// (void main should not trigger LLVM verification error)

fn main() {
    let x = 42;
    let y = x + 1;
}

// Expected output: (nothing, just exits 0)
