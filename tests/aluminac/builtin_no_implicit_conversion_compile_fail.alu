// ALUMINAC_FLAGS: --sysroot sysroot
// EXPECTED_ERROR: type mismatch
// Distinct builtin types never convert implicitly (as in alumina-boot); an
// explicit `as` is needed.

fn main() {
    let a: u32 = 1;
    let b: i32 = a;
}
