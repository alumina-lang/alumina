// ALUMINAC_FLAGS: --sysroot sysroot
// EXPECTED_ERROR: type hint required
// A generic parameter that can be inferred neither from the arguments nor
// from the context is an error (as in alumina-boot); it used to be `()`.

fn main() {
    let x = Option::none();
}
