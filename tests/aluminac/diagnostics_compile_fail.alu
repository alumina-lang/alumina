// Test: diagnostics
// Compile-fail test for error reporting
// COMPILE_FAIL: could not resolve field 'z'

struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let p = Point { x: 1, y: 2 };
    p.z;
    0
}
