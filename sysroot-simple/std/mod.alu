// Simplified standard library for self-hosted compiler bootstrap
// This avoids protocol bounds, `when` expressions, and other features
// that the self-hosted compiler doesn't support yet.

mod option;
mod mem;
mod cmp;
mod collections;
