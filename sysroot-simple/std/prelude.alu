// Prelude: items available everywhere without explicit imports
use std::option::Option;
use std::result::{try, Result};

// Formatting macro internals need to be in scope at call sites because
// aluminac expands macros textually without module-scoped resolution.
use std::fmt::{static_format_args, _fold_result};
