// Stub std::fmt for self-hosted compiler bootstrap
// Only provides type declarations used by imports

struct Formatter {
    _dummy: i32,
}

struct Error {
    _dummy: i32,
}

type Result = std::result::Result<(), Error>;

fn write() {
    // stub
}
